import numpy as np

arr1 = np.array([1, 2, 3, 4, 5])
arr2 = np.array([5, 4, 3, 2, 1])

add_result = np.add(arr1, arr2)
print(f"Addition is {add_result}")

sub_result = np.subtract(arr1, arr2)
print(f"Subtraction is {sub_result}")
